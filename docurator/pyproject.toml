[tool.poetry]
name = "docurator"
version = "0.1.0"
description = ""
authors = ["Ivernoerve-mbp14 <ino033@uit.no>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
